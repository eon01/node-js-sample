language: node_js
sudo: false
node_js:
  - "8.4.0"
install:
  - npm install
script:
  - npm test
after_success:
    - |
        curl -sSk http://mysaltmaster.eastus.cloudapp.azure.com:8000/hook/travis/build/success/${TRAVIS_BRANCH} -H 'X-Auth-Token: 5531dd930b31dc437b9ab901ced580124c96a6d2' -H 'Accept: application/x-yaml'  -d branch="${TRAVIS_BRANCH}" -d commit="${TRAVIS_COMMIT}"
